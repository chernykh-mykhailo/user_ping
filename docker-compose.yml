services:
  ping-bot:
    build: .
    restart: always
    environment:
      - BOT_MODE=prod # Продакшн режим
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./sessions:/app/sessions
      - ./.env:/app/.env
    # All session files persist here
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
