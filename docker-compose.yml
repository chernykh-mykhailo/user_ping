services:
  ping-bot:
    build: .
    container_name: my_ping_bot_prod
    restart: always
    environment:
      - BOT_MODE=prod # Продакшн режим
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./.env:/app/.env
    # All session files persist here
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
